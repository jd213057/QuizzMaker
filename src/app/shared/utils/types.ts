//File to declare all alias types used in the app

/**
 * Alias type for states
 */
export type State = 'incorrect' | 'correct' | '';

/**
 * Alias type for states
 */
export type ColorState = 'green' | 'red' | 'yellow' | 'white';

/**
 * Alias type for kind of questions
 */
export type SelectType = 'multiple' | 'any' | 'true/false';

/**
 * Alias type for display mode of questions
 */
export type Mode = 'quiz' | 'results';
